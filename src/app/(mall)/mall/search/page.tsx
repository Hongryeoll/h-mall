export const dynamic = 'force-dynamic';

import ListWrapper from '@/components/search/ListWrapper';

export default function SearchPage() {
  return (
    <div className="p-4">
      <ListWrapper />
    </div>
  );
}
